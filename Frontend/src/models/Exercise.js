export class Exercise {
    constructor(name, desc, imageUrl, length, sets, reps) {
      this.name = name;
      this.desc = desc;
      this.imageUrl = imageUrl;
      this.length = length;
      this.sets = sets;
      this.reps = reps;
    }
  }

  export default Exercise;